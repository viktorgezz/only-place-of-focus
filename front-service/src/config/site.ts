export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "only focus",
  description: "focus",
  navItems: [
    {
      label: "Home",
      href: "/",
    }
  ],
  navMenuItems: [],
  links: {}
};
